node_modules/
*.log
# build output, rebuilt at publish via the prepublishOnly hook, never committed
dist/
# runtime artifacts (screenshots, traces, heap snapshots, recorder buffers)
/tmp/cdp-toolkit/
# ad hoc probe scripts, written inside the repo root so bun can resolve ./src
*.scratch.ts
# internal launch / planning docs, kept locally, NOT part of the public repo
ANALYSIS.md
LAUNCH-PLAN.md
